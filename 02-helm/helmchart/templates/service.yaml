apiVersion: v1
kind: Serivce
metadata:
  name: {{ .Values.name }}-service
  labels:
    app: {{ .Values.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.http_port }}
      targetPort: http
      name: http
  selector:
    app: {{ .Values.name }}